@page
@model IndexModel
@{
    ViewData["Title"] = "Úvodní stránka";
}

<div class="w3-container w3-center" style="background-image: url('img/background.png'); background-size: cover; background-repeat: no-repeat; background-position: center">
    <h1>Karavanový svět</h1>
    <p>Vítejte v Karavanovém světě - průvodci cestováním karavanem! Objevujte s námi neomezenou svobodu na cestách, získejte odborné rady pro řízení karavanů a inspirujte se tipy na úžasné výlety. Zajistěte si hladký a nezapomenutelný zážitek na každé cestě. Připojte se ke komunitě vášnivých karavanistů a objevte svět s pohodlím na čtyřech kolech!</p>
</div>

<div class="w3-container">
    @{
        var i = 1;
        var imgStyle = "";
        foreach (var item in Model.LatestBlogPosts)
        {
            if (i % 2 != 0)
            {
                imgStyle = "left;margin-right:0.5em";
            }
            else
            {
                imgStyle = "right";
            }
            <div class="w3-card w3-padding w3-margin-bottom w3-hover-pale-green">
                <img src="@item.BlogImage.ImagePath" alt="@item.BlogImage.AltText" class="w3-border w3-padding w3-image w3-hover-grayscale" style="max-width:400px;float:@imgStyle" />
                <h2><a asp-page="./Blog/Details" asp-route-id="@item.Blog.Id">@item.Blog.Title</a></h2>
                <h4>Datum zveřejnění: @item.Blog.PublishDate.ToLongDateString()</h4>
                <p>@item.Blog.Perex</p>
            </div>
            i++;
        }
    }
</div>
